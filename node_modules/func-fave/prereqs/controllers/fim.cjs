const { isCallable } = require('./callable.cjs');
const { FIF } = require('./fif.cjs');

function FIM(
  func,
  params = []
) {
  return FIF(
    ( isCallable(func) )
    , 
    () => func(...params),
    () => 0
  )
}

module.exports = {
  FIM
}