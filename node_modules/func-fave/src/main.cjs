const { FIF } = require('../prereqs/controllers/fif.cjs');

const { FDEC } = require('../prereqs/controllers/fdec.cjs');

const { FIM } = require('../prereqs/controllers/fim.cjs');

const { FEQ } = require('../prereqs/controllers/operators/feq.cjs');

const { FGR } = require('../prereqs/controllers/operators/fgr.cjs');

const { FLR } = require('../prereqs/controllers/operators/flr.cjs');

const { FFOR } = require('../prereqs/controllers/iterators/ffor.cjs');

const { createIterator } = require('../prereqs/controllers/iterators/fit.cjs');

const { createEdge } = require('../prereqs/controllers/models/edge.cjs');

const { createNode } = require('../prereqs/controllers/models/node.cjs');

const { isCallable } = require('../prereqs/controllers/callable.cjs');

module.exports = {
  FIF,
  FDEC,
  FIM,

  FEQ,
  FGR,
  FLR,

  FFOR,

  createIterator,
  createEdge,
  createNode,

  isCallable
}